dry_run = true
manual_halt = true
capital = { min_deposit_confirmations = 2, max_automatically_deployable_usdc = 100.0, yearly_deployment_cap_usdc = 500.0, cumulative_deployment_cap_usdc = 1000.0 }
pacing = { min_order_usdc = 5.0, max_order_usdc = 25.0, deposit_cooldown_seconds = 3600, target_horizon_days = 365 }
schedule = { utc_hour = 12, utc_minute = 0, weekdays = [1, 3, 5] }
hyperliquid = { endpoint = "https://example.invalid", account_env = "HYPE_ACCOUNT_ID", signing_key_env = "HYPE_SIGNING_KEY" }
execution = { max_order_usdc = 25.0, max_slippage_bps = 20, order_timeout_seconds = 30 }
